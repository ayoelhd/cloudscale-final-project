{
  "name": "cloudscale-app",
  "version": "1.0.0",
  "description": "CloudScale containerized web application – Final Project",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "node -e \"const http = require('http'); const req = http.get('http://localhost:3000/health', (res) => { process.exit(res.statusCode === 200 ? 0 : 1); }); req.on('error', () => process.exit(1));\" "
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
